export const isValidStudentObj = (studentObj) => {
  return true
}

export const isValidRecruiterObj = (recruiterObj) => {
  return true
}

export const isValidCollegeAdminObj = (recruiterObj) => {
  return true
}

export const isValidZoomEmail =  (email) => {
  return true
}